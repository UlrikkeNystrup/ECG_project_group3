package ProducerConsumer;

// det er ikke nødvendigt at pakke data ind i en Message klasse, men det gør vi
public class Message {
    private int id;
    private double data;

    // constructors and getter/setters
}
